package com.luxoft.gandzha.peopledirectoryservice.repository;

/*
public class EmployeeRepositoryTest {

    @MockBean
    EmployeeRepository employeeRepository;

    @Test
    void findByNameTest() {
        Employee employee = new Employee(1l, "Denis", "Gandzha",
                LocalDate.of(1994, 05, 05), "Programmer",
                "460-45-98", "dg@mail.com");

        employeeRepository.save(employee);
        assertEquals(employee.getName(), employeeRepository.findByName(employee.getName()).getName());
    }
}
*/